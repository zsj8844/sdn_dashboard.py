# SDN拓扑跨网段不通 — 问题与解决

## 现象
iot1/iot2/iot4 能 ping 通 iot3（网关），但不能 ping 通 h1/h2。

## 根因（两个 bug）

**Bug 1：ARP 包让控制器崩溃**
- `ble_switch_13.py` 里 `_forward()` 当了，`ip_pkt` 为 None（ARP 包不是 IPv4），却直接写了 `ip_dst = ip_pkt.src`
- 后果：iot3 想做 ARP 解析 h1 的 MAC 地址，ARP 包到 s1 控制器处理时崩溃，ARP 永远回不来，ICMP 永远发不出

**Bug 2：控制器被动模式，没预装流表**
- 控制器等 Packet-In 才下发流表，一个 ping 来回 4 跳，等流表建立完 ping 早就超时了

## 修复
1. `_forward()` 加 None 判断，ARP 走泛洪不崩溃
2. 交换机上线时控制器主动预装 IPv4 双向流表（control_http.py + ble_switch_13.py）

## 结果
全通，0% loss，<1ms 延迟。

## 深层问题：没有真正扩展 BLE Mesh 协议

代码里有 `IoTProcessor`、`_parse_ble()`、`encode_openflow_extension()` 等 BLE 扩展框架，但全都是**骨架代码**：

- 网关把 BLE 数据编码成 OpenFlow Experimenter 字段发给控制器
- 控制器收到后只是打印日志、推给 Web 面板，**没有根据 BLE 数据类型/优先级去影响路由决策或流表下发**
- BLE 扩展字段实际上没有参与任何 OpenFlow 协议层面的处理

当前能通是因为装了**普通的 IPv4 静态流表**，跟 BLE Mesh 协议扩展无关。BLE Experimenter 扩展这一层还没真正打通。

## 交换机本身没有做特殊修改

OVS 交换机是标准的 OpenFlow 1.3 交换机，没有任何自定义扩展。所有逻辑全在控制器一侧：

- **控制器应用下发**（`create_edge_application`/`deploy_application_to_device`）：代码定义了接口，但没有真正把应用推送到交换机或设备上执行
- **边缘应用管理**（`AppDeploymentManager`）：仅有框架，未实现实际的边缘设备应用部署流程
- **设备角色管理**（`DeviceRoleManager` + `switch_device_role`/`register_device_with_role`）：接口定义了，但没有实际的设备角色切换逻辑

简而言之，交换机只是普通 OVS，控制器里的扩展管理模块都是**接口骨架，没有落地**。

## Vue 拓扑只显示两台交换机，不显示主机

**根因**：装主动流表后，IPv4 包命中流表直接转发，不再触发 Packet-In → `_handle_ip_packet()` 不再被调用 → `discover_device()` 不会执行 → `topology['hosts']` 永远是空的 → Vue 拿不到主机数据。

ARP 包虽然触发 Packet-In，但原来没有处理 ARP 中的 IP 信息去做主机发现。

**修复**：在 `_packet_in_handler` 中加了 ARP 解析，从 ARP 包里提取 `src_ip` / `src_mac` 调用 `discover_device()`。重启控制器后，ARP 交换期间主机就会被发现并显示在拓扑图上。

## Vue 拓扑中 IoT 设备有节点但无连线

**现象**：iot1/iot2/iot4 在拓扑图上显示为"?"标记节点，但没有到交换机或网关的连线。

**根因**：iot1/iot2/iot4 直连 iot3，不在 SDN 交换机集群内：

```
IoT 子网（非 SDN）          SDN 集群
┌─────────────┐      ┌──────────────────┐
│ iot1        │      │                  │
│ iot2 ── iot3 ───── s1 ── s2 ── h1/h2 │
│ iot4        │      │                  │
└─────────────┘      └──────────────────┘
```

控制器通过 Packet-In 发现设备时会记录 `switch_dpid` 和 `port`，画的连线就是"设备—交换机端口"。iot1/iot2/iot4 的流量只走直连 veth pair，从不经过 s1/s2 → `switch_dpid=0` → Vue 不画线。

**这是正确的行为**，不是 bug。IoT 设备确实不在 OVS 交换机上，无连线才是真实的拓扑。要改的话需要额外画 iot → iot3(网关) 的逻辑连线。

---

## 待解决的架构问题（4 个）

| # | 问题 | 层面 | 一句话 |
|---|------|------|--------|
| ① | **网关没有控制通道** | 控制面 | iot3 不是 OVS，无法跟控制器注册/问路/收推送 |
| ② | **网关没有 BLE 协议栈** | 接入层 | iot3 只做 IP 转发，听不懂 BLE 广播，传感器数据进不来 |
| ③ | **OpenFlow 全在用户态** | 数据面 | 流表处理在 ovs-vswitchd 用户态，高并发首包延迟堆积 |
| ④ | **Experimenter 未入交换机** | 协议层 | BLE 扩展字段只走 HTTP→Web面板，没嵌入 `OFPFlowMod` |

```
优先级： ② BLE接入 → ① 网关控制面 → ③ 内核下沉 → ④ Experimenter入交换机
理由：  没数据一切白搭 → 网关可控 → 转发变快 → 协议完整性
```

---

## 问题① — 网关没有控制通道

iot3 物理上连 s1，但自身是普通 Linux 主机，不走 OpenFlow。控制面到网关这里断了。

```
控制器 ←→ s1（OVS, OpenFlow）←→ s2（OVS, OpenFlow）
  ↑
  ║  OpenFlow 看不到网关
  ════════════════════════════
  ↓
iot3 ── iot1/iot2/iot4
  ↑
  只有 IP 数据面，没有控制面
```

| 缺口 | 表现 |
|------|------|
| 无法注册 | 控制器不知道 iot3 是网关、下面挂了哪些设备 |
| 无法问路 | iot3 转发时只能自己查 ARP/路由表，不知道控制器规划的路径 |
| 无法下推 | 控制器 FlowMod 只到 s1/s2，到不了网关 |
| 无法上报 | IoT 设备增减、状态变化，控制器不知情 |

**目标**：给网关加一个控制通道（HTTP / WebSocket），让它能注册、问路、收推送。

**现有基础**：`ControlHTTPServer(:16634)` 已有 `/install-path` 和 `/install-all`，但只监听 `127.0.0.1`。

---

## 问题② — 网关没有 BLE Mesh 协议栈

iot3 在配置中被标为 "BLE/IoT协议网关"，实际上只能做 IP 转发。真实 IoT 传感器只发 BLE 广播，网关听不懂。

```
BLE 传感器（广播）  →  iot3（仅IP）  →  SDN 交换机  →  控制器
   ✅ 有数据              ❌ 不收              ✅            ✅ 能处理但没数据
```

**好消息**：控制器侧的管道已就绪——`IoTProcessor` 能解析 BLE type/value 映射为 priority/route，`IoTExtensionManager` 能序列化为 Experimenter 二进制。只差数据源。

---

## 问题③ — OpenFlow 全在用户态，性能不足

```
首包 → ovs-vswitchd(用户态) → OpenFlow TCP → Ryu(用户态) → FlowMod → 内核转发
```

每跳都有用户态/内核态切换。IoT 传感器大量定时上报时，首包延迟会堆积。

**最直接方案**：OVS 内核 fast-path（`system` datapath），流规则命中后内核直接转发，不经过 ovs-vswitchd。当前流表多是 `ipv4_dst=xxx` 精确匹配，内核缓存命中率极高。

**进阶方案**：eBPF/XDP 在网卡驱动层查表转发（极限低延迟 <100us），或 P4 卸载到智能网卡。

---

## 问题④ — Experimenter 字段未嵌入交换机消息

BLE 扩展字段的二进制数据只做了 `POST /api/iot-extension → Web面板`，没有通过 `OFPActionExperimenter` 打入 `OFPFlowMod` 发给 OVS。控制器已通过 priority/route 影响转发决策，这更多是协议完整性的事，优先级最低。

```
当前：BLE数据 → Experimenter序列化 → HTTP → Web面板 ✅
                                     ↘ OVS     ❌

目标：BLE数据 → Experimenter序列化 → HTTP → Web面板 ✅
                                     ↘ OFPActionExperimenter → OVS ✅
```

---

## 实施路线图

### 总优先级

```
② BLE接入 ──→ ① 网关控制面 ──→ ③ 内核下沉 ──→ ④ Experimenter入交换机
  (P0)          (P1)              (P2)            (P3)
```

没有 BLE 数据进来，后面所有优先级调度、内核快转、多级流表都是空转。

---

### P0: BLE 数据接入

**目标**：网关能收到 BLE 传感器广播 → 数据进 `IoTProcessor.process()`

**Step 1: 硬件确认**
```bash
# iot3 是什么设备？有 BLE 吗？
hciconfig / lsusb
# 没有 → USB BLE dongle（CSR8510/CC2540, ¥20-50）
```

**Step 2: 协议栈选型**

| 方案 | 特点 | 适合 |
|------|------|------|
| PyGATT + BLE scan | 极简 Python，直接读广播 | **快速原型（1-2天）** |
| BlueZ mesh | Linux 原生，Python D-Bus API | 生产版 |
| NimBLE (Apache) | C 实现，轻量稳定 | 生产版 |
| ESP32 BLE Mesh | SIG 标准硬件实现 | 长期独立节点 |

**建议**：先用 PyGATT 跑通数据流，再换 BlueZ/NimBLE 加固。

**Step 3: 数据管道**
```
网关 BLE scan → {"ble_type":"temp","ble_value":45.2} → HTTP POST → 控制器
                                                                  ↓
                                                        IoTProcessor.process()
                                                                  ↓
                                                        priority→FlowMod, route→选路
                                                        Experimenter→Web面板
```

**验证清单**：

| 验证项 | 通过标准 |
|--------|----------|
| 网关扫到 BLE | `hcitool lescan` 看到传感器 |
| 数据到控制器 | `IoTProcessor.process()` 收到 ble_type + ble_value |
| 优先级联动 | 温度>80→priority=5, 温度<30→priority=2 |
| 路由联动 | priority≥4 走 route_b |
| Web面板 | 扩展字段卡片显示 sensor_type/priority/route |

---

### P1: 网关控制通道

**目标**：网关能向控制器注册/问路/上报

**Step 1: 控制器 API 升级**
```
ControlHTTPServer 升级：
  ├─ 监听 0.0.0.0:16634（iot子网可达）
  ├─ POST /api/register       — 网关注册 + 下属设备列表
  ├─ GET  /api/path?dst=...   — 返回优选下一跳（含备用）
  ├─ POST /api/device-event   — 设备上下线通知
  └─ GET  /api/health         — 心跳
```

**Step 2: 网关 sidecar (gateway_agent.py)**
```python
# 在 iot3 上跑，轻量 Python + requests，不依赖 OpenFlow
启动 → POST /api/register（我是网关，下属 iot1/iot2/iot4）
循环 → GET  /api/path?dst=h1 → 控制器返回 route_a/route_b
     → POST /api/device-event（设备上下线）
```

**Step 3（可选）: WebSocket 推模式**
- 网关推送：BLE 紧急事件实时推送，不用轮询
- 控制器下推：路由变更通知网关即时切换

---

### P2: OpenFlow 内核下沉

**Step 1: 诊断**
```bash
lsmod | grep openvswitch               # 内核模块?
ovs-vsctl get bridge s1 datapath_type  # netdev 还是 system?
ovs-appctl dpif/show                   # 当前 datapath 详情
```

**Step 2: 切换**
```bash
modprobe openvswitch
ovs-vsctl set bridge s1 datapath_type=system
ovs-vsctl set bridge s2 datapath_type=system
```

**Step 3: 验证**
```bash
ovs-dpctl show              # 内核 datapath 是否存在
ovs-dpctl dump-flows        # 内核缓存的流规则
ping 测试首包 vs 后续包延迟   # 预期 10x+ 提升
```

**预期效果**：

| 指标 | netdev（现状） | system（目标） |
|------|---------------|---------------|
| 首包延迟 | ~10-50ms | ~2-5ms |
| 命中流延迟 | ~1-5ms | ~0.1-0.5ms |
| 控制器 CPU | 高 | 低 |

---

### P3: Experimenter 入交换机

**改动点**：
1. `PacketProcessor._forward()` — FlowMod actions 中追加 `OFPActionExperimenter`
2. `FlowManager.add_flow()` — 加 `experimenter_data` 可选参数
3. OVS 侧 — 标准 OVS 默认支持透传，无需改动

先跑通 P0+P1+P2，最后回来打这个洞。优先级最低。