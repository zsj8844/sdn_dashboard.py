# 更新日志

## v4.4.0 (2026-04-18) — 虚拟环境优化和脚本改进

**重大更新**:
1. **虚拟环境优化**：虚拟环境目录从 `venv` 改为 `.venv`，符合现代 Python 项目规范
2. **依赖管理完善**：创建完整的 `requirements.txt`，支持自动安装系统依赖
3. **脚本路径优化**：移除硬编码路径，使用动态路径计算，提高可移植性

**新增/修改文件**:

| 文件 | 类型 | 说明 |
|------|------|------|
| `create_venv.sh` | 新增 | 虚拟环境管理脚本 |
| `requirements.txt` | 新增 | 项目依赖清单 |
| `manage.py` | 新增 | 项目管理脚本 (start/stop/status/clean) |
| `run_tests.py` | 更新 | 适配 .venv 虚拟环境 |
| `iot_sdn_topology.py` | 更新 | 使用 .venv Python |

---

## v4.3.0 (2026-03-28) — IoT网关增强版完善

**主要更新**:
1. **网关日志修复**：添加 flush() 和 fsync() 确保日志持久化写入
2. **网关转发逻辑明确**：根据BLE地址智能转发到对应目标主机，同时编码OpenFlow扩展字段
3. **文档更新**：添加场景B工作原理说明

**网关转发规则**:
```
aa:bb:cc:dd:ee:01 (iot1) → 192.168.1.20 (h1)
aa:bb:cc:dd:ee:02 (iot2) → 192.168.1.21 (h2)
aa:bb:cc:dd:ee:04 (iot4) → 192.168.1.20 (h1)
```

---

## v4.2.0 (2026-02-26) — 应用下发和角色切换

**新增功能**:

1. **应用下发**：完整的边缘应用生命周期管理
   - 应用类型：数据过滤、AI推理、数据聚合、自定义
   - 功能：创建、封装、部署、卸载
   - 核心文件：`switch/extensions/app_deployment.py`, `app_field.py`

2. **角色切换**：设备模式动态切换
   - 三种模式：纯转发、计算混合、物联网网关
   - 支持能力检查、优雅过渡、自动模式识别
   - 核心文件：`switch/extensions/role_switch.py`, `role_field.py`

3. **文档**：创建功能实现详细文档，更新所有相关文档

**功能对比**:

| 功能 | v4.1 | v4.2 |
|------|------|------|
| OpenFlow 1.3 | ✅ | ✅ |
| 拓扑/流表管理 | ✅ | ✅ |
| 应用下发 | ❌ | ✅ |
| 角色切换 | ❌ | ✅ |

---

## v4.1.0 (2026-02-25) — 拓扑可视化修复

**修复内容**:
1. 修复增强版面板中拓扑可视化显示错误（iot设备现在正确连接到iot3网关）
2. 优化控制器流表处理逻辑，添加 IoT 设备 IP 地址专门处理
3. 从 git 历史恢复原始版本的拓扑文件

**修复确认**:

| 问题 | 状态 |
|------|------|
| 拓扑可视化显示错误 | ✅ 已修复 |
| iot1/iot2/iot4 连接关系 | ✅ 已修正 |
| 控制器流表处理逻辑 | ✅ 已优化 |
| 拓扑文件语法错误 | ✅ 已恢复 |

---

## v4.0.0 (2026-02-24) — 架构简化（纯NAT网关）

**重大架构变更 — 纯NAT网关模式**:

1. **移除网关模拟器**：完全删除 `iot_gateway_simulator.py`，使用纯 iptables NAT 转发
2. **直接发送模式**：IoT设备直接发送数据到目标主机，通过 iot3 NAT 转发
3. **控制器优化**：简化流表下发逻辑，确保所有IP数据包正确处理

**新数据流向**:
```
IoT设备 → iot3 (NAT网关+iptables) → s1 → s2 → h1/h2
```

**测试命令更新**:
```bash
# 直接发送到目标主机
mininet> iot1 echo "..." | nc -u 192.168.1.20 5005
mininet> iot2 echo "..." | nc -u 192.168.1.21 5005
mininet> iot4 echo "..." | nc -u 192.168.1.20 5005
```

**架构优势**：复杂度降低，iot1和iot4可正常发送，无网络转发干扰，更符合真实网络架构。

---

## v3.1.0 (2026-02-24) — 网关模拟器重写

1. 完全重写 `iot_gateway_simulator.py`，实现完整的接收和转发功能
2. 网关监听 5005 端口，根据设备地址智能转发
3. 实现同时接收IoT数据和发送模拟数据的双模式

---

## v3.0.0 (2026-02-24) — 传统网关模式

**拓扑结构升级**:
- iot1/iot2/iot4 → iot3（网关）→ s1 → s2 → h1, h2
- 多子网架构：不同设备位于不同子网
- iot3 启用 IP 转发（4个接口，分别连接各设备）
- 子网配置：iot1(192.168.2.x), iot2(192.168.3.x), iot4(192.168.4.x)

---

## v2.0.0 (2026-02-22) — 拓扑重构

**修复内容**:
1. 重构拓扑：所有设备连接到 s1 交换机（标准SDN拓扑）
2. 修复 iot3 多接口同IP问题
3. 优化网络配置和连通性测试
4. 移除多接口同IP的配置，简化网络栈

---

*最后更新：2026-04-18*
