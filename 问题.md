# BLE Mesh SDN 项目 — 问题日志

---

## 一、已修复

### 1. 跨网段不通（iot↛h1/h2）

**根因**：两个 bug。
- ARP 包到控制器后 `ip_pkt` 为 None，`ip_pkt.src` 崩溃
- 控制器被动模式，ping 超时前流表来不及建

**修复**：`_forward()` 加 None 判断，ARP 走泛洪；交换机上线主动预装 IPv4 双向流表。

**结果**：全通，0% loss，<1ms。

---

### 2. Vue 拓扑不显示主机

**根因**：主动流表上线后 IPv4 包不再触发 Packet-In，`discover_device()` 永远不执行。

**修复**：Packet-In 中加 ARP 解析，从 ARP 提取 src_ip/src_mac 调 `discover_device()`。

---

### 3. Vue 拓扑 IoT 设备无连线

**根因**：iot1/iot2/iot4 直连 iot3 网关，不经过 s1/s2，`switch_dpid=0`，前端不画线。

**结论**：不是 bug，拓扑正确。要改需额外画 iot→iot3 逻辑连线。

```
IoT 子网                          SDN 集群
┌─────────────┐          ┌──────────────────┐
│ iot1        │          │                  │
│ iot2 ── iot3 ───────── s1 ── s2 ── h1/h2 │
│ iot4        │          │                  │
└─────────────┘          └──────────────────┘
```

---

### 4. BLE 扩展骨架未落地

`IoTProcessor`、`IoTExtensionManager`、`AppDeploymentManager`、`DeviceRoleManager` 等模块定义了完整接口和序列化管线，但缺少：
- 真实 BLE 数据源（见问题 ①）
- 数据真正嵌入 OpenFlow 消息（见问题 ③）
- 边缘应用实际推送和设备角色切换逻辑

当前网络能通是因为装了普通的 IPv4 静态流表，与 BLE 无关。

---

### 5. Experimenter 字段已嵌入流表 ✅

**修复**：`packet_processor.py` 的 `_forward()` 中，下发 FlowMod 时在 actions 里追加 `OFPActionExperimenterUnknown`。BLE 数据（传感器类型、优先级、路由）编码为 Experimenter 二进制嵌入 OVS 流表动作，`ovs-ofctl dump-flows` 可直接看到 `experimenter(id=0x12345678,data=...)`。

---

## 二、待解决（2 个）

### 问题 ① — 网关没有 BLE Mesh 协议栈

iot3 配置中标注为 "BLE/IoT协议网关"，实际只能 IP 转发，没有 BLE 协议栈。

```
BLE 传感器（广播）→ iot3（仅IP）→ SDN 交换机 → 控制器
   ✅ 有数据         ❌ 不收       ✅           ✅ 能处理但没数据
```

**好消息**：控制器侧管道已就绪。`IoTProcessor.process(ble_type, ble_value)` → priority/route → `IoTExtensionManager` → Experimenter 二进制。只差数据从网关进来。

**方向**：两种测试方式。
- 只测控制器管道：写脚本直接 POST 模拟 BLE 数据 → `IoTProcessor`，不需要硬件
- 端到端真实 BLE：网关用物理机（非 Mininet namespace）插 USB dongle，或 ESP32 独立节点采集后喂给控制器

---

### 问题 ② — OpenFlow 转发全在用户态

```
首包 → ovs-vswitchd(用户态) → Ryu控制器(用户态) → FlowMod → 内核datapath
```

每跳都有态切换开销，大量 IoT 定时上报时首包延迟堆积。

**方向**：OVS 切到内核 `system` datapath，命中流表直接内核转发；后续可上 eBPF/XDP。

---

## 三、实施路线

```
① BLE接入(P0) → ② 内核下沉(P1)
```

理由：没数据一切白搭 → 转发变快。

---

### P0: BLE 数据接入

**目标**：网关收到 BLE 传感器广播，数据进入 `IoTProcessor.process()`。

**Step 1 — 硬件**
```bash
hciconfig / lsusb          # iot3 有 BLE 吗？
# 没有 → USB BLE dongle (CSR8510/CC2540, ¥20-50)
```

**Step 2 — 协议栈**：先用 PyGATT 快速原型（1-2 天出数据），再换 BlueZ mesh 或 NimBLE 加固。

**Step 3 — 数据管道**
```
网关 BLE scan → {"ble_type":"temp","ble_value":45.2} → HTTP POST → 控制器
                                                                  ↓
                                                        IoTProcessor.process()
                                                          priority→FlowMod
                                                          route→选路
                                                          Experimenter→Web面板
```

**验证**：网关扫到广播 → 控制器收到数据 → 优先级和路由随数值联动 → Web 面板可见。

---

### P1: OpenFlow 内核下沉

**诊断**
```bash
lsmod | grep openvswitch               # 内核模块加载了没
ovs-vsctl get bridge s1 datapath_type  # netdev 还是 system
ovs-appctl dpif/show
```

**切换**
```bash
modprobe openvswitch
ovs-vsctl set bridge s1 datapath_type=system
ovs-vsctl set bridge s2 datapath_type=system
```

**验证**
```bash
ovs-dpctl show       # 内核 datapath
ovs-dpctl dump-flows # 内核缓存流表
```

| 指标 | netdev（现状） | system（目标） |
|------|---------------|---------------|
| 首包延迟 | ~10-50ms | ~2-5ms |
| 命中流延迟 | ~1-5ms | ~0.1-0.5ms |

