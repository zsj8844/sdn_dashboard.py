# SDN IoT 扩展项目

## 项目概述

这是一个基于Ryu控制器的软件定义网络(SDN)物联网(IoT)扩展项目，实现了BLE Mesh网络的数据收集、转发和可视化监控功能。

## 最新更新（2026-04-18）

### 🚀 重大更新 - v4.4.0

1. **虚拟环境优化**：
   - 将虚拟环境目录从 `venv` 改为 `.venv`，符合现代 Python 项目规范
   - 支持多种虚拟环境检测顺序：项目内 `.venv` → conda 环境 → 系统 Python
   - 提供完整的虚拟环境管理脚本 `create_venv.sh`

2. **依赖管理完善**：
   - 创建 `requirements.txt` 文件，包含所有项目依赖
   - 支持自动安装系统依赖（如 Open vSwitch）
   - 提供依赖验证功能

3. **脚本路径优化**：
   - 将所有脚本中的硬编码路径改为相对路径
   - 使用动态路径计算，提高项目可移植性
   - 支持在任意目录执行脚本

4. **文档全面更新**：
   - 更新所有文档以反映虚拟环境变更
   - 操作手册更新启动命令
   - 测试文档更新测试流程

### 🚀 重大更新 - v4.3.0 (2026-03-28)

1. **IoT网关增强版完善**：
   - 修复网关日志记录问题，添加文件强制刷新机制
   - 确保所有启动信息和接收数据都正确记录到日志
   - 改进异常处理，确保异常信息也被记录
   - 优化日志写入性能，使用flush和fsync确保数据持久化

2. **网关转发逻辑明确**：
   - 明确两种数据传输方式：直接转发和网关处理
   - 网关根据BLE地址智能转发到对应目标主机
   - 网关同时编码OpenFlow扩展字段并发送到控制器

3. **文档全面更新**：
   - 更新测试文档，添加场景B工作原理说明
   - 添加详细的网关转发机制解释
   - 更新所有相关文档以反映最新代码变更

### 🔧 重要修复 - v4.2.1 (2026-03-XX)

1. **网关日志修复**：
   - 问题：网关接收数据后没有正确记录到gateway.log
   - 原因：日志写入没有立即刷新，且启动信息没有记录
   - 解决：添加flush()和fsync()，确保所有信息都被记录

2. **Web面板日志说明**：
   - Web面板使用独立的内存记录日志
   - 网关日志和Web面板日志是两套独立的系统

### 🚀 重大更新 - v4.2.0 (2026-02-26)

1. **应用下发功能实现**：提供完整的边缘应用生命周期管理，包括应用创建、封装、部署、卸载，支持数据过滤、AI推理、数据聚合等多种应用类型
2. **角色切换功能实现**：支持设备模式动态切换，提供纯转发、计算混合、物联网网关三种模式，支持模式切换前后的能力检查和优雅过渡
3. **扩展字段增强**：新增应用部署和角色切换相关的OpenFlow扩展字段
4. **文档全面更新**：创建详细的功能实现文档，更新所有相关文档

### 🔧 重要修复 - v4.1.0 (2026-02-25)

1. **拓扑可视化修复**：修复增强版面板中拓扑显示错误，iot1/iot2/iot4 现在正确连接到 iot3 网关
2. **控制器流表优化**：添加对 IoT 设备 IP 地址的专门处理，确保数据正确转发
3. **拓扑文件恢复**：从 git 历史恢复原始版本的拓扑文件，修正语法错误
4. **数据传输说明**：明确两种数据传输方式（通过网关模拟器和直接发送）

### 🔧 重大更新 - v4.0.0（2026-02-24）

1. **架构简化**：完全移除网关模拟器，使用纯 iptables NAT 网关转发
2. **直接发送**：IoT设备直接发送数据到目标主机，通过 iot3 的 NAT 转发
3. **控制器优化**：简化流表下发逻辑，确保所有IP数据包正确处理
4. **所有文档更新**：反映最新的纯NAT网关架构

### 📊 拓扑结构（IoT网关增强版）

```
iot1 (192.168.2.10) ─┐
iot2 (192.168.3.11) ───┼── iot3 (IoT网关) ─── s1 ─── s2 ─── h1, h2
iot4 (192.168.4.13) ──┘
```

**子网配置**：
- iot1: 192.168.2.10/24，网关 192.168.2.1
- iot2: 192.168.3.11/24，网关 192.168.3.1
- iot4: 192.168.4.13/24，网关 192.168.4.1
- iot3: 4 个接口，分别连接 s1、iot1、iot2、iot4，启用 iptables NAT 和 IoT网关应用

### 📊 数据流向（两种模式）

#### 模式A：直接转发（纯网络层）
```
IoT设备 (iot1/iot2/iot4) 
  ↓ (UDP/5005 直接发送到 h1/h2)
iot3 (NAT网关 - iptables转发)
  ↓ (IP转发和NAT)
s1 (OpenFlow13交换机)  ← 控制器下发流表
  ↓ (OpenFlow协议)
s2 (OpenFlow13交换机)  ← 控制器下发流表
  ↓ (UDP/5005)
h1/h2 (监控主机)
```

#### 模式B：网关处理（应用层智能转发）
```
IoT设备 (iot1/iot2/iot4) 
  ↓ (UDP/5005 发送到 iot3网关对应接口)
iot3 (IoT网关应用 - 监听5005端口)
  ├─→ 解析数据（BLE地址、传感器类型、数据值）
  ├─→ 编码OpenFlow扩展字段
  ├─→ 发送扩展信息到控制器
  └─→ 根据BLE地址智能转发到 h1/h2
                          ↓
                    s1/s2 (OpenFlow13交换机)
                          ↓
                    h1/h2 (监控主机)
```

### 测试命令

#### 方式一：IoT网关处理（推荐，支持OpenFlow扩展）
```bash
# 1. 在 h1/h2 上启动监听
mininet> xterm h1 h2
# 在h1窗口输入：nc -ul 5005
# 在h2窗口输入：nc -ul 5005

# 2. 查看网关日志（另一个终端）
mininet> iot3 tail -f logs/gateway.log

# 3. 从 iot1 发送数据到 iot3 网关（对应接口IP）
mininet> iot1 echo "aa:bb:cc:dd:ee:01,temp,25.5" | nc -u 192.168.2.1 5005

# 4. 从 iot2 发送数据到 iot3 网关
mininet> iot2 echo "aa:bb:cc:dd:ee:02,humidity,65.3" | nc -u 192.168.3.1 5005

# 5. 从 iot4 发送数据到 iot3 网关
mininet> iot4 echo "aa:bb:cc:dd:ee:04,pos,(30.12,120.45)" | nc -u 192.168.4.1 5005

# 预期结果：
# - 网关日志显示接收、解析、编码、转发全过程
# - h1/h2收到对应数据
# - Web面板显示数据传输日志
```

**网关转发规则**：
- `aa:bb:cc:dd:ee:01` (iot1) → `192.168.1.20` (h1)
- `aa:bb:cc:dd:ee:02` (iot2) → `192.168.1.21` (h2)
- `aa:bb:cc:dd:ee:04` (iot4) → `192.168.1.20` (h1)

#### 方式二：直接转发（纯网络层，需要 iptables NAT）
```bash
# 1. 在 h1/h2 上启动监听
mininet> xterm h1 h2
# 在h1窗口输入：nc -ul 5005
# 在h2窗口输入：nc -ul 5005

# 2. 测试iot1 → h1（直接发送到h1）
mininet> iot1 echo "aa:bb:cc:dd:ee:01,temp,25.5" | nc -u 192.168.1.20 5005

# 3. 测试iot2 → h2（直接发送到h2）
mininet> iot2 echo "aa:bb:cc:dd:ee:02,humidity,65.3" | nc -u 192.168.1.21 5005

# 4. 测试iot4 → h1（直接发送到h1）
mininet> iot4 echo "aa:bb:cc:dd:ee:04,pos,(30.12,120.45)" | nc -u 192.168.1.20 5005
```

## 目录结构

```
ryu_extend/
├── switch/                 # 控制器扩展模块
│   ├── ble_switch_13.py    # BLE Mesh交换机控制器（已优化）
│   ├── extensions/          # OpenFlow扩展字段实现
│   │   ├── manager.py       # 扩展字段管理器
│   │   ├── sensor_field.py  # 传感器类型字段
│   │   ├── priority_field.py # 设备优先级字段
│   │   ├── route_field.py   # 路由选择字段
│   │   └── base_field.py    # 基类定义
│   └── test/               # 测试脚本
├── templates/              # Web监控面板
│   ├── sdn_dashboard.py    # 后端服务
│   └── dashboard.html      # 前端界面
├── topology/               # 网络拓扑脚本
│   └── iot_sdn_topology.py # Mininet拓扑定义（已更新，移除网关模拟器）
├── scripts/                # 辅助脚本
├── docx/                   # 项目文档
├── start_project.sh        # 一键启动脚本
├── run_tests.sh           # 测试脚本
├── core_ctl.sh            # 核心控制脚本
├── create_venv.sh         # 虚拟环境管理脚本
└── requirements.txt       # 项目依赖文件
```

## 功能特性

### 核心功能
- **BLE数据转发**: 自动识别和转发BLE Mesh网络数据包
- **智能流表管理**: 动态下发OpenFlow流表规则
- **实时监控面板**: Web界面展示网络状态和设备信息
- **日志收集**: 实时收集和展示BLE设备数据
- **连通性检测**: 自动检测网络设备连通状态
- **OpenFlow扩展**: 支持传感器类型、设备优先级、路由选择、应用部署、角色切换等扩展字段
- **纯NAT网关**: 使用iptables实现网关转发，符合真实网络架构
- **应用下发**: 提供完整的边缘应用生命周期管理（创建、部署、卸载）
- **角色切换**: 支持设备模式动态切换（纯转发/计算混合/物联网网关）

### 技术特点
- 基于OpenFlow 1.3协议
- 支持RESTful API接口
- 响应式Web界面设计
- 多线程异步处理
- 完善的日志系统
- Experimenter扩展机制

## 快速开始

### 环境要求
- Ubuntu 20.04 LTS 或更高版本
- Python 3.8+
- Ryu控制器 4.34
- Mininet 2.3.0
- Open vSwitch 2.13+

### 安装依赖

```bash
# 使用自动脚本创建虚拟环境并安装依赖
chmod +x create_venv.sh
./create_venv.sh

# 激活虚拟环境
source .venv/bin/activate
```

### 手动安装（备用）

```bash
# 安装 Ryu 控制器
pip install ryu

# 安装 Mininet (Ubuntu/Debian)
sudo apt-get install mininet

# 或者从源码安装 Ryu (推荐用于开发)
git clone https://github.com/faucetsdn/ryu.git
cd ryu
pip install .
```

### 手动部署

1. **启动控制器**
```bash
cd ryu_extend
ryu-manager switch/ble_switch_13.py --verbose --ofp-tcp-listen-port 6634
```

2. **启动Web面板**
```bash
python3 templates/sdn_dashboard.py
```

3. **启动网络拓扑**
```bash
sudo python3 topology/iot_sdn_topology.py
```

## 使用说明

### 访问监控面板
启动成功后，访问 `http://localhost:5000` 查看监控界面

### 数据传输测试

#### 步骤1：在目标主机启动监听
```bash
mininet> xterm h1 h2

# 在h1窗口输入
nc -ul 5005

# 在h2窗口输入
nc -ul 5005
```

#### 步骤2：测试IoT设备
```bash
# 测试iot1 → h1（直接发送到h1）
mininet> iot1 echo "aa:bb:cc:dd:ee:01,temp,25.5" | nc -u 192.168.1.20 5005

# 测试iot2 → h2（直接发送到h2）
mininet> iot2 echo "aa:bb:cc:dd:ee:02,humidity,65.3" | nc -u 192.168.1.21 5005

# 测试iot4 → h1（直接发送到h1）
mininet> iot4 echo "aa:bb:cc:dd:ee:04,pos,(30.12,120.45)" | nc -u 192.168.1.20 5005
```

### API接口

#### 接收BLE日志
```
POST /api/logs
Content-Type: application/json

{
    "ble_addr": "aa:bb:cc:dd:ee:01",
    "type": "temperature",
    "value": "25.6",
    "src_ip": "192.168.1.10"
}
```

#### 健康检查
```
GET /health
```

### 网络拓扑命令行参数

```bash
# 基本使用
sudo python3 topology/iot_sdn_topology.py

# 指定控制器地址
sudo python3 topology/iot_sdn_topology.py --controller-ip 192.168.1.100 --controller-port 6653

# 调试模式
sudo python3 topology/iot_sdn_topology.py --debug

# 不清理现有网络
sudo python3 topology/iot_sdn_topology.py --no-cleanup
```

## 架构说明

### 数据流向（纯NAT网关模式）
```
IoT设备(iot1/iot2/iot4) → iot3(NAT网关) → OpenFlow交换机(s1/s2) → 监控主机(h1/h2)
  ↓                           ↓                              ↓
直接发送目标IP        iptables NAT转发              控制器下发流表
```

### 组件交互
1. **IoT设备**（iot1/iot2/iot4）通过UDP端口5005直接发送数据到目标主机（h1/h2）
2. **iot3网关**使用iptables NAT和IP转发功能转发数据包
3. **交换机(s1/s2)** 捕获数据包并发送给Ryu控制器
4. **控制器**解析数据包并下发流表规则
5. **Web面板**通过API接收日志并在前端展示
6. **连通性检测**通过OVS流表分析网络状态

## OpenFlow扩展字段说明

### 支持的扩展字段

| 字段名称 | 字段ID | 说明 |
|----------|--------|------|
| SensorType | 1 | 传感器类型（温度、湿度、光照等） |
| DevicePriority | 2 | 设备优先级（1-5级） |
| RouteSelect | 3 | 路由选择（路径A、路径B） |
| AppDeployment | 4 | 应用部署字段（应用ID、版本、部署状态） |
| RoleSwitch | 5 | 角色切换字段（设备模式、切换状态） |

### 传感器类型
- 1: 温度传感器
- 2: 湿度传感器
- 3: 光照传感器
- 4: 移动传感器
- 5: 压力传感器
- 6: 气体传感器
- 7: 声音传感器

### 优先级级别
- 1: 低优先级
- 2: 普通优先级
- 3: 高优先级
- 4: 关键优先级
- 5: 紧急优先级

## 配置说明

### 控制器配置 (ble_switch_13.py)
- `BLE_ADDR_MATCH`: BLE地址匹配字段
- `BLE_OUTPUT_ACTION`: BLE输出动作
- `MY_EXPERIMENTER_ID`: 实验者ID (0xdeadbeef)

### Web面板配置 (sdn_dashboard.py)
- `DEVICES`: 网络设备定义
- `GATEWAY_LOG`: 网关日志缓存
- `S1_LOG`: s1交换机日志缓存
- `S2_LOG`: s2交换机日志缓存

### 拓扑配置 (iot_sdn_topology.py)
- `ip_map`: 各主机的IP地址配置（h1, h2）
- iot1/iot2/iot4 配置不同子网的IP地址
- iot3 网关配置4个接口，启用IP转发功能和iptables NAT规则

## 故障排除

### 常见问题

1. **控制器无法启动**
   - 检查Ryu是否正确安装
   - 确认端口6634未被占用
   - 查看控制器日志文件

2. **Web面板无法访问**
   - 检查5000端口是否开放
   - 确认防火墙设置
   - 查看dashboard.log日志

3. **网络连通性问题**
   - 使用`sudo mn -c`清理网络
   - 检查OVS服务状态
   - 验证控制器连接
   - 检查iot3网关的IP转发功能是否启用
   - 确认各设备的默认网关配置正确

4. **网关转发问题**
   - 检查iot3的sysctl配置：`sysctl net.ipv4.ip_forward`
   - 验证iot3的4个接口IP配置正确
   - 检查各IoT设备的默认网关指向iot3
   - 使用ping测试各子网之间的连通性
   - 查看网关日志：`tail -f /tmp/iot_gateway_simulator.log`

5. **IoT设备无法发送数据**
   - 确认数据直接发送到目标主机（h1: 192.168.1.20, h2: 192.168.1.21）
   - 检查iot3的iptables NAT规则是否正确配置
   - 使用ping测试各设备之间的连通性

### 日志文件位置
- 控制器日志: `~/ryucontronl/logs/controller.log`
- Web面板日志: `~/ryucontronl/logs/dashboard.log`
- 拓扑日志: `~/ryucontronl/logs/topology.log`

### 调试命令
```bash
# 查看交换机流表
ovs-ofctl dump-flows s1 -O OpenFlow13

# 查看控制器连接
ovs-vsctl show | grep Controller

# 在h1启动监听
xterm h1 nc -ul 5005

# 抓包分析
mininet> iot3 xterm
# 然后运行: tcpdump -i any -n 'udp port 5005'
```

## 开发指南

### 代码规范
- 使用Python 3标准库
- 遵循PEP 8编码规范
- 添加适当的日志记录
- 编写清晰的注释

### 扩展建议
1. 添加更多传感器类型支持
2. 实现数据持久化存储
3. 增加安全认证机制
4. 支持多控制器部署
5. 实现NETCONF协议协同
6. 添加路径规划算法

### 添加新的扩展字段
1. 在 `switch/extensions/` 中创建新的字段类
2. 继承 `IoTExtensionField` 基类
3. 实现 `serialize()` 和 `parse()` 方法
4. 在 `manager.py` 中注册新字段

## 历史更新

### v4.2 (2026-02-26)
- ✅ 新增应用下发功能（应用创建、部署、卸载）
- ✅ 新增角色切换功能（纯转发/计算混合/物联网网关模式）
- ✅ 新增应用部署和角色切换扩展字段
- ✅ 创建详细的功能实现文档
- ✅ 更新所有相关文档

### v4.1 (2026-02-25)
- ✅ 修复拓扑可视化显示问题
- ✅ iot1/iot2/iot4 现在正确连接到 iot3 网关
- ✅ 优化控制器流表处理逻辑
- ✅ 恢复拓扑文件原始版本
- ✅ 明确两种数据传输方式

### v4.0 (2026-02-24)
- ✅ 完全移除网关模拟器，简化架构
- ✅ 使用纯iptables NAT网关转发
- ✅ IoT设备直接发送到目标主机
- ✅ 优化控制器流表逻辑
- ✅ iot1/iot4现在可以正常发送数据到h1
- ✅ 更新所有文档

### v3.1 (2026-02-24)
- ✅ 网关模拟器完全重写，实现完整接收和转发功能
- ✅ 智能转发（按设备地址）
- ✅ 更新所有文档

### v3.0 (2026-02-24)
- ✅ 拓扑结构升级为传统网关模式
- ✅ iot3网关功能增强，启用IP转发
- ✅ 多子网架构，不同设备位于不同子网
- ✅ 优化连通性测试逻辑
- ✅ 更新所有文档

### v2.0 (2026-02-22)
- ✅ 重构拓扑结构，所有设备连接到s1交换机
- ✅ 修复iot3多接口同IP问题
- ✅ 优化网络配置和连通性测试
- ✅ 更新文档和目录结构

### v1.0 (初始版本)
- 基础SDN IoT框架
- BLE数据转发功能
- Web监控面板
- OpenFlow扩展字段基础实现

## 许可证

本项目仅供学习和研究使用。

## 联系方式

如有问题或建议，请提交issue或联系项目维护者。
