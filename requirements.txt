# SDN物联网控制系统依赖文件
# 基于Ryu控制器的SDN项目
# 推荐 Python 3.10+

# 构建依赖（必须先安装，确保 ryu 构建兼容性）
setuptools<60
wheel
pbr

# 核心依赖
ryu==4.34
# mininet 需通过系统包管理器安装: sudo apt install mininet

# Web面板依赖
Flask
flask-cors

# 网络和数据处理
scapy
requests

# 开发工具
pytest
flake8
black

# 系统依赖（通过apt安装）
# sudo apt install -y python3-pip python3-venv openvswitch-switch openvswitch-common mininet
